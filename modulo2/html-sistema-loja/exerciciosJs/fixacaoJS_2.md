```
    function calculaPrecoTotal(quantidade) {
    let macas;
    if (quantidade >= 12) {
        macas = quantidade * 1
    } else {
        macas = quantidade * 1.3
    }
    return macas
    }

```